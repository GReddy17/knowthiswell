# No environment variables are required today. This file exists so the
# pattern is established before it's actually needed (e.g. when
# analytics, a contact form, or a third-party API integration is added).

# NEXT_PUBLIC_SITE_URL=https://knowthiswell.com
