node_modules/
.next/
out/
.env*.local
*.log
.DS_Store
.vercel
*.tsbuildinfo

# Operational tooling / account-mapping data — not for the public repo.
# Skills still work locally (Claude Code reads .claude/skills from disk,
# not from git), this just keeps them out of version control.
.claude/skills/
.claude/private/
.agents/
skills-lock.json

# Dropped custom TTS+animation video experiment (Piper/Kokoro/Welly) —
# 500MB+ of local render assets, not part of the deployed site.
video-pipeline/

# Local mirror of feedback-form submissions, synced from Vercel Blob via
# `npm run sync-feedback` — never committed, may contain visitor-submitted
# text and IP addresses.
/feedback/
